name: agua_viva
description: Aplicativo para avaliação de nascentes.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '^3.7.0'  # Restrição específica do SDK conforme solicitado

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  shared_preferences: ^2.2.0
  uuid: ^3.0.7
  image_picker: ^1.0.0
  intl: ^0.18.1
  cupertino_icons: ^1.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.2

flutter:
  uses-material-design: true
